server:
  port: 8083

spring:
  application:
    name: review-service

  datasource:
    url: jdbc:mysql://localhost:3306/swiftcart_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: pass

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
